name: Research EoT Swiss 4006 comparison

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compare:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install pinned Swiss Ephemeris wrapper
        run: python -m pip install --disable-pip-version-check pyswisseph==2.10.3.2
      - name: Download official Swiss planetary ephemerides
        run: |
          mkdir -p tmp/sweph
          curl -fsSL --retry 3 --retry-delay 2 \
            https://raw.githubusercontent.com/aloistr/swisseph/master/ephe/sepl_18.se1 \
            -o tmp/sweph/sepl_18.se1
          curl -fsSL --retry 3 --retry-delay 2 \
            https://raw.githubusercontent.com/aloistr/swisseph/master/ephe/sepl_36.se1 \
            -o tmp/sweph/sepl_36.se1
          sha256sum tmp/sweph/sepl_18.se1 tmp/sweph/sepl_36.se1
      - name: Compare hourly production EoT against Swiss reference
        env:
          SWISSEPH_EPHE_PATH: tmp/sweph
          OUTPUT_DIR: tmp/eot-swiss-4006
        run: node scripts/research-compare-eot-swiss.mjs
      - name: Upload research evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: eot-swiss-4006-comparison
          path: tmp/eot-swiss-4006/
          if-no-files-found: warn
          retention-days: 7
          compression-level: 6
